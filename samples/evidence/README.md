# Redacted evidence sample

This directory contains only small JSON summaries used to exercise the CPU-only evidence generator in CI. It contains no camera frames, labels, model files, TensorRT engines, local paths, or environment manifests.

The values are a redacted representation of the bounded 50-image TensorRT FP16 result. They are illustrative release-sample content, not a substitute for the locally retained provenance artifacts.
